# 遊戲素材

這裡只放目前遊戲使用的素材：`characters/luxue/`（露雪）、`characters/lianmiao/`（戀喵）、`cards/luxue/`、`monsters/forest/` 與 `scenes/`。角色、怪物的引用集中在 `src/data/art.ts`，卡圖引用集中在 `src/data/cardArt.ts`。

未使用的舊圖、原圖和草稿放在根目錄的 `art-previews/`，舊製作紀錄放在 `docs/history/art/`。新增候選圖先放預覽區，採用後才複製到 `assets/`。
